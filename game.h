#include <vector>
#include <card.h>
#include <cstdlib>

class game{
	vector<card> first_show(deck &d){
		vector<card> first_five;
		srand(time(NULL));
			int irand = rand() % 54;
			if(irand > 51){
				
			}
			else{

			}
	}
};